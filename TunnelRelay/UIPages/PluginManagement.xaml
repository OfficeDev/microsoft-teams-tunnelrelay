<Window x:Class="TunnelRelay.PluginManagement"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TunnelRelay"
        mc:Ignorable="d"
        Title="Plugin Management" Height="700" Width="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>

        <!--Top controls-->
        <TextBlock Text="Configure plugins" FontSize="20" FontWeight="SemiBold" Grid.Column="1" Grid.Row="1" />
        <TextBlock Text="Select the plugins you want to configure and set the settings for the same" Grid.Column="1" Grid.Row="2" />
        
        
        <!--Left List views-->
        <ListView Grid.Row="4" Grid.Column="1" />
        <StackPanel Name="stcPanel"/>
    </Grid>
</Window>
